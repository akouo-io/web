/**
 * @akouo/ui — shared React component library.
 *
 * Scaffold placeholder: a single export proving the package resolves from the
 * apps. Real components land here later; components will build on tokens from
 * `@akouo/theme`, never the other way around (theme ← ui ← apps).
 */
export const UI_PACKAGE = "@akouo/ui" as const;
